#include<iostream>
#include<vector>

using std::vector;

int main() {
	vector<int> {42, 42, 42, 42, 42, 42, 42, 42, 42, 42};
	vector<int> (10, 42);

	vector<unsigned> kun;
	unsigned j = 0;
	for (auto i = 0; i != 10; ++i)
		kun.push_back(j);
}